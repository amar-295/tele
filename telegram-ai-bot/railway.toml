[build]
builder = "NIXPACKS"

[deploy]
# Use "--mode telegram" to start the Telegram bot instead.
startCommand = "python main.py --mode api"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
